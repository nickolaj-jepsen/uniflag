[package]
name = "uniflag-render"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "uniflag rendering primitives — flag effects, brightness state machine, host-testable Surface trait."

[dependencies]
proto = { path = "../proto" }

[lints]
workspace = true
