[package]
name = "proto"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "uniflag wire-protocol types — shared by the firmware, uniflag-cli, and (as a C# mirror) the SimHub plugin."

[lints]
workspace = true
